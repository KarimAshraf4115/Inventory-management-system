BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Returns] ADD [Return_Date] DATETIME2 NOT NULL CONSTRAINT [Returns_Return_Date_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
